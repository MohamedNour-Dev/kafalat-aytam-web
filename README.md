# 🌙 مركز أمل لليتيم (Amal Orphan Center)

موقع إلكتروني تعريفي وتفاعلي لمؤسسة "أمل لليتيم" الخيرية، مصمم بتجربة مستخدم عصرية وأنيقة تهدف إلى تسهيل عملية كفالة الأيتام ونشر الوعي حول أثر التبرعات.

---

## ✨ المميزات (Features)

- **تصميم عصري وفاخر**: واجهة مستخدم داكنة (Dark Theme) مع لمسات "Emerald" الجمالية.
- **نظام كفالة تفاعلي**: نافذة منبثقة (Modal) تتيح للمستخدم اختيار نوع الكفالة وإدخال تفاصيل الدفع وسجل الهاتف.
- **التعرف التلقائي على الدول**: حقل رقم الهاتف مزود بقائمة دول وخيار إدخال يدوي يكتشف علم الدولة تلقائياً بناءً على الرمز الدولي.
- **عدادات الإنجازات**: تأثيرات حركية عند الوصول لقسم أرقام المؤسسة (أطفال مكفولون، منح تعليمية، إلخ).
- **نظام صوتي تفاعلي**: موسيقى خلفية هادئة تعمل تلقائياً عند أول تفاعل للمستخدم مع الصفحة.
- **متجاوب تماماً**: يعمل الموقع بكفاءة عالية على جميع الشاشات (Desktop, Tablet, Mobile).
- **التحقق من البيانات**: نظام فحص فوري للمدخلات (الاسم، البريد، رقم الهاتف) لضمان صحة البيانات.

---

## 🧠 المنطق البرمجي (Technical Logic Description)

تم بناء الموقع باستخدام **Vanilla JavaScript** مع التركيز على الكفاءة وتجربة المستخدم، وإليك لمحة عن كيفية عمل الكود:

### 1. نظام الصوت التفاعلي (Interactive Audio System)

بسبب قيود المتصفحات على التشغيل التلقائي، يعتمد الكود على "المراقبة الذكية". يقوم الموقع بمراقبة أي تفاعل أولي (نقر، تمرير، لمس) ليقوم فوراً بتشغيل الموسيقى الخلفية ومزامنة الأيقونة في شريط التنقل.

### 2. محرك الكشف عن الدول (Country Detection Engine)

يحتوي نموذج الكفالة على منطق متقدم للتعرف على كود الدولة:

- يتم البحث في خريطة بيانات (Mapping) لأكثر من 50 دولة.
- يقوم الكود بمطابقة أطول بادئة ممكنة (3 أرقام مثل 966، ثم رقمين مثل 20) لإظهار العلم الصحيح تلقائياً فور كتابة الرقم يدوياً.

### 3. معالج الحركات (Animation Controller)

يستخدم الكود `IntersectionObserver` بدلاً من الأحداث التقليدية للتمرير، مما يضمن أداءً عالياً. يقوم بتشغيل العدادات الرقمية وحركات الظهور (Fade-in) فقط عندما تصبح العناصر مرئية للمستخدم.

### 4. نظام التحقق من المدفوعات (Payment Validation Logic)

تم دمج منطق برمجى لمحاكاة عملية دفع حقيقية:

- **خوارزمية لوهن (Luhn Algorithm)**: يتم فحص أرقام البطاقات البنكية في الوقت الفعلي للتأكد من صحتها برمجياً قبل البدء في معالجة الطلب.
- **تنسيق تلقائي**: يتم تنسيق رقم البطاقة (إضافة مسافات كل 4 أرقام) وتنسيق تاريخ الانتهاء تلقائياً أثناء كتابة المستخدم.

### 5. الرصد الذكي للتنقل (Scroll Spy System)

يعتمد شريط التنقل على نظام رصد ذكي يقوم بتغيير الرابط "النشط" (Active Link) تلقائياً أثناء تمرير المستخدم بين الأقسام المختلفة، مما يحسن من تجربة التصفح.

---

## 🛠 التقنيات المستخدمة (Tech Stack)

- **HTML5**: لهيكلة المحتوى بشكل دلالي (Semantic HTML).
- **CSS3**: تم استخدام تقنيات متقدمة مثل:
  - Flexbox & CSS Grid.
  - متغيرات الألوان (CSS Variables).
  - تأثيرات الزجاج (Glassmorphism).
  - حركات الظهور (Scroll Reveal Animations).
- **JavaScript (Vanilla)**: البرمجة التفاعلية بدون استخدام مكتبات خارجية ثقيلة، لضمان السرعة والخفة.
- **Font Awesome**: للأيقونات الاحترافية.
- **Google Fonts (Cairo)**: للخط العربي المميز.

---

## 📂 هيكلة المجلدات (Project Structure)

```text
Orphan-Sponsor/
├── index.html        # الملف الأساسي للموقع
├── style.css         # ملف التنسيقات والأنماط
├── script.js        # منطق العمل والتفاعلات
└── assets/           # الموارد والوسائط
    ├── images/       # الصور المستخدمة
    ├── sounds/       # ملفات الصوت
    ├── icons/        # الأيقونات والشعارات
    └── fonts/        # الخطوف الخاصة (إن وجدت)
```

---

## 🚀 كيفية التشغيل (How to Run)

1.  قم بتحميل المشروع (Clone).
2.  افتح ملف `index.html` في أي متصفح حديث.

---

## 🤝 المساهمة (Contribution)

نرحب بالمساهمات! إذا كان لديك أي اقتراح لتحسين الموقع، لا تتردد في فتح (Issue) أو إرسال (Pull Request).

---

## ✍️ مطور المشروع (Author)

- **محمد نور (Mohammed Noor)** - _التطوير البرمجي والتصميم_

---

## 📄 الترخيص (License)

هذا المشروع مرخص بموجب رخصة **MIT** - انظر ملف [LICENSE](LICENSE) لمزيد من التفاصيل.

---

> **تم تطوير هذا المشروع كنموذج إبداعي لدعم قضايا الأيتام.** 🤍
